# Credentials for local development ONLY
# Never commit production credentials
# These are the ONLY source of truth — never hardcode credentials elsewhere.

sql_server:
  sa_password: "Dev@12345"

mongodb:
  root_user: "admin"
  root_password: "Dev@12345"

redis:
  password: "Dev@12345"

rabbitmq:
  user: "admin"
  password: "Dev@12345"
